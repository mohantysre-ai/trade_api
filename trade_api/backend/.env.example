# Angel One SmartAPI credentials (https://smartapi.angelone.in/)
REDACTED=REDACTED
REDACTED=REDACTED
REDACTED=REDACTED
REDACTED=REDACTED

# Optional
MARKET_API_HOST=0.0.0.0
MARKET_API_PORT=8000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
MARKET_FILTER_PROMPT=Select the top 10 stocks from the live Angel One universe using momentum, liquidity, relative strength, and catalyst quality.
INTRADAY_CANDIDATE_LIMIT=20

# Optional LLM summarization (set if you want AI news summaries)
# Use `openai` for OpenAI-compatible endpoints or `gemini` for Google's Gemini models.
LLM_PROVIDER=gemini
REDACTED=
REDACTED=
LLM_API_URL=
LLM_MODEL=gemini-2.5-flash
