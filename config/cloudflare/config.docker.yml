# Auto-generated / maintained for Docker tunnel
# calendar.sigq.in → Panchang; sigq.in → IROS frontend
tunnel: iros-desk
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: calendar.sigq.in
    service: http://panchang-api:8088
  - hostname: sigq.in
    service: http://frontend:3000
  - service: http_status:404
